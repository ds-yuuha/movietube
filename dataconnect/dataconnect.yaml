specVersion: "v1"
serviceId: "movietube"
location: "asia-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "movietube-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
